<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.FinSchoolApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:statusBarColor">@color/white</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <style name="Theme.SplashScreen" parent="Theme.FinSchoolApp">
        <item name="windowSplashScreenBackground">@color/main_background</item>
        <item name="postSplashScreenTheme">@style/Theme.FinSchoolApp</item>
    </style>

    <style name="Theme.FinSchoolApp" parent="Base.Theme.FinSchoolApp" />
</resources>